import Dimensions from 'Dimensions';

const DEVICE_WIDTH = Dimensions.get('window').width;
const DEVICE_HEIGHT = Dimensions.get('window').height;
const MARGIN = 40;
const SIZE = 40;
const API = 'http://api.sunsurfers.ru/api/v1/';
const API_TOKEN = 'aldjfkladsjfklsdjfoi2ri13jr';

export {
  DEVICE_WIDTH,
  DEVICE_HEIGHT,
  MARGIN,
  SIZE,
  API
};
