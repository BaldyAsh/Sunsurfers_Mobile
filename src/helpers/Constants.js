import Dimensions from 'Dimensions';

const DEVICE_WIDTH = Dimensions.get('window').width;
const DEVICE_HEIGHT = Dimensions.get('window').height;
const MARGIN = 40;
const SIZE = 40;
const API = 'http://private-anon-143d9a011d-sunsurfers.apiary-mock.com/api/v1/';
const API_TOKEN = '';

export {
  DEVICE_WIDTH,
  DEVICE_HEIGHT,
  MARGIN,
  SIZE,
  API
};
