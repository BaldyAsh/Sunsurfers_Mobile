import { StyleSheet } from 'react-native';

const inputsForm = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  }
});

export {
  inputsForm
};
